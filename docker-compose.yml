version: "3.9"

services:
  flask:
    build: .
    container_name: deepface-api
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped